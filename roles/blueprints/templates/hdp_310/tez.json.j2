##########################
# 1, HOST GROUP DEFINITIONS
##########################


# GATEWAY
{% macro host_group_gateway(joint,blueprint) -%} {{ joint() }}
                {
                    "name": "TEZ_CLIENT"
                }               
{%- endmacro %}


# AGENT-MASTER(HA)
{% macro host_group_ha_master(joint,blueprint) -%} {{ joint() }}
                {
                    "name": "TEZ_CLIENT"
                }
{%- endmacro %}


# AGENT-MASTER
{% macro host_group_master(joint,blueprint) -%} {{ joint() }}
                {
                    "name": "TEZ_CLIENT"
                }
{%- endmacro %}



# AGENT-SLAVE
{% macro host_group_slave(joint,blueprint) -%} {{ joint() }}
                {
                    "name": "TEZ_CLIENT"
                }
{%- endmacro %}



##########################
# 2, CONFIGURATIONS
##########################

{% macro configurations(joint,blueprint) -%} {{ joint() }}
    {
      "tez-site": {
        "properties": {
          "tez.am.resource.memory.mb": "2048",
          "tez.runtime.io.sort.mb": "819",
          "tez.runtime.unordered.output.buffer.size-mb": "184",
          "tez.lib.uris": "/hdp/apps/${hdp.version}/tez/tez.tar.gz"
        }
      }
    }
{%- endmacro %}

##########################
# 3, KERBEROS DESCRIPTORS
##########################
{% macro kerberos_descriptor_services(joint,kerberos) -%}
{%- endmacro %}